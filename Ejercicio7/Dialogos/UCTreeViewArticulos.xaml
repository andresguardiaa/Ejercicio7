<UserControl x:Class="Ejercicio7.Dialogos.UCTreeViewArticulos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ejercicio7.Dialogos"
             xmlns:ent="clr-namespace:Ejercicio7.Backend.Modelo"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             x:Name="treeViewArticulos"
             Loaded="treeViewArticulos_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <TextBlock Text="Árbol de Artículos" FontSize="24" FontWeight="Bold" Margin="10" HorizontalAlignment="Center"/>
        </Grid>
        <Grid DockPanel.Dock="Left">
            <TreeView ItemsSource="{Binding listaArticulos}">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type ent:Articulo}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CarTireAlert"/>
                            <TextBlock Text="{Binding Numserie}" Margin="10,0,0,0"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Grid>
        <Grid>
            
        </Grid>
    </DockPanel>
</UserControl>
